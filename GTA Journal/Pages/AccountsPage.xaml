<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="GTA_Journal.Pages.AccountsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GTA_Journal.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="20, 20, 20, 20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Text="Аккаунты" Style="{StaticResource TitleLargeTextBlockStyle}"></TextBlock>
            <Button Click="AddAccountButton_Click" Margin="0,10,0,0" Grid.Column="1" Style="{StaticResource AccentButtonStyle}">
                <StackPanel Orientation="Horizontal">
                    <FontIcon Glyph="&#xE8FA;" />
                    <TextBlock Text="Добавить аккаунт" Margin="8,0,0,0" />
                </StackPanel>
            </Button>
        </Grid>

        <TextBlock Grid.Row="1" Style="{StaticResource CaptionTextBlockStyle}">Выбранный пользователь помечается меткой</TextBlock>

        <Grid Grid.Row="2" Margin="0, 20, 0, 0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel x:Name="NoAccountsPanel" Visibility="Collapsed" Grid.Row="0" VerticalAlignment="Center">
                <FontIcon HorizontalAlignment="Center" Margin="10" Glyph="&#xE783;" />
                <TextBlock HorizontalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}">Аккаунтов нет</TextBlock>
            </StackPanel>

            <StackPanel x:Name="LoadingPanel" Grid.Row="0" VerticalAlignment="Center">
                <ProgressRing IsActive="True" />
            </StackPanel>

            <ListView Grid.Row="0" SelectionChanged="UserListView_SelectionChange" Visibility="Collapsed" x:Name="UserListView">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0" Orientation="Horizontal">
                                <PersonPicture Height="45" VerticalAlignment="Center" ProfilePicture="{Binding AvatarUrl}" Margin="10" DisplayName="{Binding Username}" />
                                <StackPanel VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" Text="{Binding Username}" />
                                        <TextBlock VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="3,0,0,0" Visibility="{Binding IsAdmin}" Text="(Админ)" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock VerticalAlignment="Center" Style="{StaticResource CaptionTextBlockStyle}" Text="Время окончания:" />
                                        <TextBlock VerticalAlignment="Center" Margin="3,0,0,0" Style="{StaticResource CaptionTextBlockStyle}" Text="{Binding Expires}" />
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                            <Button Grid.Column="1" Margin="10,0,0,0" Click="UserListViewDeleteUser_Click" Tag="{Binding}">
                                <FontIcon Glyph="&#xE74D;" ToolTipService.ToolTip="Удалить аккаунт" />
                            </Button>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <ContentDialog x:Name="AddAccountDialog" IsPrimaryButtonEnabled="False" Title="Добавление аккаунта" PrimaryButtonText="Войти" PrimaryButtonStyle="{StaticResource AccentButtonStyle}" CloseButtonText="Закрыть">
                <StackPanel Margin="0,10,0,0">
                    <TextBox x:Name="LoginTextBox" TextChanged="AddAccountFields_TextChanged" Header="Логин:" PlaceholderText="Mamut_Bobov" Margin="0,10,0,0"></TextBox>
                    <PasswordBox x:Name="PasswordTextBox" PasswordChanged="AddAccountFields_TextChanged" Header="Пароль" PlaceholderText="oral_coconut"  Margin="0,20,0,0"></PasswordBox>

                    <CheckBox x:Name="SavePasswordCheckBox" Content="Сохранить пароль (не безопасно)" Margin="0,10,0,0" />
                </StackPanel>
            </ContentDialog>

            <ContentDialog x:Name="DeleteAccountDialog" Title="Удаление аккаунта" PrimaryButtonText="Да" PrimaryButtonStyle="{StaticResource AccentButtonStyle}" CloseButtonText="Отмена">
                <TextBlock>Вы уверены, что хотите удалить аккаунт из приложения?</TextBlock>
            </ContentDialog>
        </Grid>
    </Grid>
</Page>
